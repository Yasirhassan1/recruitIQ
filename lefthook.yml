pre-commit:
  parallel: true
  commands:
    fix: 
      run: npm run biome:fix
      stage_fixed: true
      exclude: "node_modules"

commit-msg:
  parallel: true
  commands: 
   commitlint: 
     run: npx commitlint --edit {1}